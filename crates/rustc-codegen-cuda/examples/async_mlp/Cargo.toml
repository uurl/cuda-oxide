[package]
name = "async_mlp"
version = "0.1.0"
edition = "2024"
authors = ["Nihal Pasham <nihalp@nvidia.com>"]
license = "Apache-2.0"

# Mark as standalone crate (not part of parent workspace)
[workspace]

# Async MLP example: multi-kernel pipeline with DeviceOperation composition.
# Build with:
#   cargo oxide run async_mlp

[dependencies]
cuda-device = { path = "../../../cuda-device" }
cuda-host = { path = "../../../cuda-host", features = ["async"] }
cuda-core = "0.3.1"
cuda-async = "0.3.1"
tokio = { version = "1", features = ["rt", "rt-multi-thread", "macros"] }

[package]
name = "mir-projected-call-destination"
version = "0.1.0"
edition = "2024"
license = "Apache-2.0"

# Standalone crate (not part of the parent workspace).
[workspace]

# Build and run with:
#   cargo oxide run mir_projected_call_destination

[dependencies]
cuda-device = { path = "../../../cuda-device" }
cuda-host = { path = "../../../cuda-host" }
cuda-core = "0.3.1"
libm = "0.2"
